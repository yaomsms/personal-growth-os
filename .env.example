# ============================================
# Supabase 配置
# ============================================
# 请将以下值替换为你自己的 Supabase 项目配置
# 获取方式：登录 Supabase 控制台 → 进入项目 → Settings → API

# Supabase 项目 URL
# 格式: https://your-project-ref.supabase.co
VITE_SUPABASE_URL=your-supabase-project-url

# Supabase 匿名公开 Key (anon key)
# 注意：这是公开的 key，可以安全地放在前端代码中
# 千万不要使用 service_role key！
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ============================================
# 使用说明
# ============================================
# 本项目是纯静态 HTML 应用，不使用 Vite 或其他构建工具。
# 环境变量文件仅作为配置参考，实际使用时请选择以下方式之一：
#
# 方式一：修改 index.html 和 login.html
# 在 <head> 标签中添加：
#   <script>
#     window.SUPABASE_URL = '你的 URL';
#     window.SUPABASE_ANON_KEY = '你的 Key';
#   </script>
#
# 方式二：浏览器控制台设置（仅当前浏览器有效）
# 打开开发者工具 → Console，执行：
#   localStorage.setItem('pgos_supabase_url', '你的 URL');
#   localStorage.setItem('pgos_supabase_anon_key', '你的 Key');
#
# 方式三：部署时通过服务端注入
# 例如 Vercel 的环境变量 + 构建脚本注入
#
# ============================================
# 安全提示
# ============================================
# - anon key 是公开的，配合 RLS（行级安全策略）使用是安全的
# - 确保数据库已开启 RLS（DEPLOY.md 中有详细的 SQL 语句）
# - 永远不要把 service_role key 放到前端代码中
# - 定期轮换 API Key（在 Supabase 控制台中操作）
